# Benchmarks

Executable benchmarks will be added as the wire and PBF layers land. The
methodology and required metrics are defined in `../docs/BENCHMARKS.md`.

`benchmark/data/` and `benchmark/results/` are intentionally ignored. Dataset
metadata and hashes belong in a future tracked `datasets.toml`; large extracts
do not belong in Git.
